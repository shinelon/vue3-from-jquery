# lib-mode demo

Vite **库模式**示例：把一个 `MyButton` 组件打包成可发布的库（ES + UMD）。

## 跑

```bash
npm install
npm run build      # 产出 dist/lib-mode.es.js 和 dist/lib-mode.umd.js
```

对应教程：[第 11 章 · 库模式](../../docs/11-库模式.md)。
